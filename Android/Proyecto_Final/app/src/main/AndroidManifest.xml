<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyectofinal.proyecto_final">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Menu" />
        <activity android:name=".Activities.Persona.RegistroPersonas" />
        <activity android:name=".Activities.Vehiculo.RegistroVehiculo" />
        <activity android:name=".Activities.Comparendo.RegistrarComparendo" />
        <activity android:name=".Activities.Persona.RegistrarLicencia" />
        <activity android:name=".Activities.Vehiculo.RegistrarEmpresa" />
        <activity android:name=".Activities.Comparendo.BusquedaComparendo" />
        <activity android:name=".Activities.Comparendo.VerComparendo" />
        <activity android:name=".Activities.Accidente.RegistroAccidente" />
        <activity android:name=".Activities.Accidente.RegistroVehiculoAccidente" />
        <activity android:name=".Activities.Accidente.ListaAccidente" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.Accidente.MapaAccidente"
            android:label="@string/title_activity_mapa_accidente" />
    </application>

</manifest>